import { Meta } from '@storybook/addon-docs/blocks'

import { Typeset } from './components/Typeset.tsx'

<Meta title="Typography" />

export const fontFamily = {
  sans: 'var(--font-sans)',
  mono: 'var(--font-mono)',
  'pp-frama': 'var(--font-pp-frama)',
  'pp-frama-text': 'var(--font-pp-frama-text)',
  'pp-supply-mono': 'var(--font-pp-supply-mono)',
  'pp-supply-sans': 'var(--font-pp-supply-sans)',
}


# Typography

<div style={{ padding: '3rem 0' }}>
  ## pp-frama

  **Font:** PP Frama

  **Weights:** 100(thin), 200(extralight), 300(light), 400(regular), 500(medium), 700(bold), 900(black)

  <Typeset fontFamily={fontFamily['pp-frama']} />
</div>

---

<div style={{ padding: '3rem 0' }}>
  ## pp-frama-text

  **Font:** PP Frama Text

  **Weights:** 300(light), 400(regular), 700(bold)

  <Typeset fontFamily={fontFamily['pp-frama-text']} />
</div>

---

<div style={{ padding: '3rem 0' }}>
  ## pp-supply-mono

  **Font:** PP Supply Mono

  **Weights:** 200(extralight), 400(regular), 500(medium), 700(bold)

  <Typeset fontFamily={fontFamily['pp-supply-mono']} />
</div>

---

<div style={{ padding: '3rem 0' }}>
  ## pp-supply-sans

  **Font:** PP Supply Sans

  **Weights:** 200(extralight), 400(regular), 500(medium), 700(bold)

  <Typeset fontFamily={fontFamily['pp-supply-sans']} />
</div>
