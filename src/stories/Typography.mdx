import { Meta, } from '@storybook/blocks'
import { fontFamily, fontSize } from '../../tailwind.config'
import { Typeset } from './components/Typeset.tsx'

<Meta title="Typography" />


export const SampleText = "Heavy boxes perform quick waltzes and jigs.";

# Typography

<div style={{ padding: '3rem 0' }}>
  ## sans

  **Font:** Inter

  **Weights:** 100(thin), 200(extralight), 300(light), 400(regular), 500(medium), 600(semibold), 700(bold), 800(extrabold), 900(black)

  <Typeset
    fontSizes={Object.entries(fontSize).map(([label, [value]]) => [label, value])}
    fontWeight={400}
    sampleText={SampleText}
    fontFamily={fontFamily['sans']}
  />
</div>

-----

<div style={{ padding: '3rem 0' }}>
  ## mono

  **Font:** Jetbrains Mono

  **Weights:** 100(thin), 200(extralight), 300(light), 400(regular), 500(medium), 600(semibold), 700(bold), 800(extrabold), 900(black)

  <Typeset
    fontSizes={Object.entries(fontSize).map(([label, [value]]) => [label, value])}
    fontWeight={400}
    sampleText={SampleText}
    fontFamily={fontFamily['mono']}
  />
</div>

-----

<div style={{ padding: '3rem 0' }}>
  ## inter

  **Font:** Inter

  **Weights:** 100(thin), 200(extralight), 300(light), 400(regular), 500(medium), 600(semibold), 700(bold), 800(extrabold), 900(black)

  <Typeset
    fontSizes={Object.entries(fontSize).map(([label, [value]]) => [label, value])}
    fontWeight={400}
    sampleText={SampleText}
    fontFamily={fontFamily['inter']}
  />
</div>

-----

<div style={{ padding: '3rem 0' }}>
  ## jetbrains-mono

  **Font:** Jetbrains Mono

  **Weights:** 100(thin), 200(extralight), 300(light), 400(regular), 500(medium), 600(semibold), 700(bold), 800(extrabold), 900(black)

  <Typeset
    fontSizes={Object.entries(fontSize).map(([label, [value]]) => [label, value])}
    fontWeight={400}
    sampleText={SampleText}
    fontFamily={fontFamily['jetbrains-mono']}
  />
</div>

-----

<div style={{ padding: '3rem 0' }}>
  ## pp-supply-mono

  **Font:** PP Supply Mono

  **Weights:** 100(thin), 400(regular)

  <Typeset
    fontSizes={Object.entries(fontSize).map(([label, [value]]) => [label, value])}
    fontWeight={400}
    sampleText={SampleText}
    fontFamily={fontFamily['pp-supply-mono']}
  />
</div>
