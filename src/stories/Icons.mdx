import { IconGallery, Meta } from '@storybook/blocks'
import { BRAND_ICONS, Icon, UI_ICONS } from '../components/Icon'
import { IconItem } from './components/IconGallery'

<Meta title="Icons" />

# Icons

<div style={{ padding: '3rem 0' }}>
  ## UI Icons

  <div style={{ paddingTop: '1rem' }}>
    <IconGallery>
      {Object.values(UI_ICONS).map(icon => (
        <IconItem name={icon}>
          <Icon name={icon} />
        </IconItem>
      ))}
    </IconGallery>
  </div>
</div>

<div style={{ padding: '3rem 0' }}>
  ## Brand Icons

  <div style={{ paddingTop: '1rem' }}>
    <IconGallery>
      {Object.values(BRAND_ICONS).map(icon => (
        <IconItem key={icon} name={icon}>
          <Icon name={icon} />
        </IconItem>
      ))}
    </IconGallery>
  </div>
</div>
