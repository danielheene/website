import { IconGallery, Meta } from '@storybook/addon-docs/blocks'
import { BRAND_ICON, Icon, UI_ICON } from '../../../web/src/components/Icon'
import { IconItem } from '@sb//IconGallery.tsx'

<Meta title="Icons" />

# Icons

<div style={{ padding: '3rem 0' }}>
  ## UI Icons

  <div style={{ paddingTop: '1rem' }}>
    <IconGallery>
      {Object.values(UI_ICON).map(icon => (
        <IconItem name={icon}>
          <Icon name={icon} />
        </IconItem>
      ))}
    </IconGallery>
  </div>
</div>

<div style={{ padding: '3rem 0' }}>
  ## Brand Icons

  <div style={{ paddingTop: '1rem' }}>
    <IconGallery>
      {Object.values(BRAND_ICON).map(icon => (
        <IconItem key={icon} name={icon}>
          <Icon name={icon} />
        </IconItem>
      ))}
    </IconGallery>
  </div>
</div>
